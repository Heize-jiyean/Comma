<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>환자 일기 페이지</title>
    <link rel="stylesheet" href="/css/profile/dashboard.css">
    <style>
        button {
            flex: none;
            height: fit-content;
            position: sticky;
            top: 130px; /* 스크롤 시 상단에 고정 */

            padding: 10px 20px;
            background-color: #4B8673; /* 버튼 배경 색상 */
            color: white; /* 텍스트 색상 */
            border: none; /* 테두리 제거 */
            border-radius: 16px; /* 테두리 둥글게 */
            cursor: pointer; /* 커서를 포인터로 설정 */
            font-size: 16px; /* 글씨 크기 */
            margin-top: 20px; /* 상단 여백 */
        }

        button:hover {
            background-color: #367952; /* 호버 시 배경 색상 변경 */
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="profile-container">
            <%- include('./profile_components/profile_card') %>
            <button onclick="location.href='/diary/new'">새 일기 작성</button>
        </div>
        <div class="content-container">
            <%- include('../diary/diaries') %>
        </div>
    </div>
</body>
</html>

