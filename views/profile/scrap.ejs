<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>스크랩 목록 페이지</title>
    <style>
        body {
            font-family: 'Pretendard-Regular', sans-serif;
            background-color: #F4F5F8;
            margin: 0;
            padding: 0;
        }
        .scrap-container {
            background: #fff;
            border-radius: 20px;
            border: 1px solid #D6D7DA;
            margin: 20px auto;
            max-width: 800px;
            display: flex;
            flex-direction: column;
            margin: 20px auto;
        }
        .tab-container {
            width: 100%; 
            display: flex;
            justify-content: center;
            border-bottom: 1px solid #D6D7DA;
        }
        .tab {
            flex: 1;
            text-align: center;
            padding: 10px 20px;
            cursor: pointer;
        }
        .content-container {
            padding: 20px;
        }
        .profile-entry {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
            border-bottom: 1px solid #D6D7DA;
            padding-bottom: 10px;
        }
        .profile-info {
            flex-grow: 1;
            margin-left: 20px;
        }
        .profile-action {
            text-align: right;
        }
        button {
            padding: 5px 10px;
            border-radius: 5px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
            border: none;
        }
        button:hover {
            background-color: #45a049;
        }
        .profile-picture {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="scrap-container">
        <div class="tab-container">
            <div class="tab active" onclick="location.href='/profile/scrap/myScraps'">내가 스크랩</div>
            <div class="tab" onclick="location.href='/profile/scrap/scrapsOnMe'">나를 스크랩</div>
        </div>
        <div class="content-container">
            <% scrapUserList.forEach(function(scrapUser) { %>
                <div class="profile-entry">
                    <img src="<%= scrapUser.profile_picture %>" alt="Profile Image" class="profile-picture">
                    <div class="profile-info">
                        <%= scrapUser.nickname %> <br>
                        <%= scrapUser.id %>
                    </div>
                    <div class="profile-action">
                        <button>스크랩 해제</button>
                    </div>
                </div>
            <% }); %>
        </div>
        
    </div>
</body>
</html>
